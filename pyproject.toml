[project]
name        = "smartdirs"
description = "📂 Better Platform Directories"
authors     = [{name="Tremeschin"}]
version     = "0.0.1"
readme      = "readme.md"
license     = "MIT"
requires-python = ">=3.10"
dependencies = [
    "attrs",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
